<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mera Music Widget</title>
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            background: #1a1a1a;
            font-family: Arial, sans-serif;
        }
        .widget {
            background: #2c2c2c;
            width: 300px;
            height: 300px;
            border-radius: 20px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
            display: flex;
            flex-direction: column;
            overflow: hidden;
            position: relative;
        }
        .album-art {
            width: 120px;
            height: 120px;
            border-radius: 10px;
            margin: 20px 0 0 20px;
            cursor: pointer;
            object-fit: cover;
        }
        .default-text {
            position: absolute;
            top: 150px;
            left: 150px;
            right: 20px;
            color: #e0e0e0;
            font-size: 14px;
            text-align: left;
        }
        .controls {
            display: none;
            position: absolute;
            bottom: 20px;
            left: 20px;
            right: 20px;
            text-align: center;
        }
        .controls button {
            background: #444;
            border: none;
            color: #fff;
            font-size: 18px;
            padding: 8px 12px;
            margin: 0 5px;
            border-radius: 50%;
            cursor: pointer;
        }
        .song-title {
            display: none;
            color: #e0e0e0;
            font-size: 16px;
            text-align: center;
            position: absolute;
            bottom: 60px;
            left: 20px;
            right: 20px;
        }
        .album-list {
            display: none;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(0, 0, 0, 0.9);
            border-radius: 20px;
            padding: 20px;
            color: #e0e0e0;
            overflow-y: auto;
        }
        .album-list h3 {
            margin: 0 0 10px;
        }
        .album-list ul {
            list-style: none;
            padding: 0;
        }
        .album-list li {
            padding: 10px;
            cursor: pointer;
            border-bottom: 1px solid #444;
        }
        .album-list li:hover {
            background: #444;
        }
        .playing .default-text {
            display: none;
        }
        .playing .controls, .playing .song-title {
            display: block;
        }
    </style>
</head>
<body>
    <div class="widget" id="widget">
        <img src="https://via.placeholder.com/120?text=Music" class="album-art" id="albumArt" alt="Album Art">
        <div class="default-text" id="defaultText">Abhi Koi Song Nahi 🎵<br>Play Karo! 😎</div>
        <div class="song-title" id="songTitle"></div>
        <div class="controls" id="controls">
            <button onclick="previousSong()">⏮</button>
            <button onclick="togglePlayPause()">▶️</button>
            <button onclick="nextSong()">⏭</button>
        </div>
        <div class="album-list" id="albumList">
            <h3 id="albumName"></h3>
            <ul id="albumSongs"></ul>
        </div>
        <audio id="audioPlayer"></audio>
    </div>

    <script>
        const audioPlayer = document.getElementById('audioPlayer');
        const albumArt = document.getElementById('albumArt');
        const defaultText = document.getElementById('defaultText');
        const songTitle = document.getElementById('songTitle');
        const controls = document.getElementById('controls');
        const widget = document.getElementById('widget');
        const albumList = document.getElementById('albumList');
        const albumName = document.getElementById('albumName');
        const albumSongs = document.getElementById('albumSongs');
        let isPlaying = false;

        // Sample songs with albums
        const playlists = [
            {
                album: "Bollywood Hits",
                art: "https://via.placeholder.com/120?text=Bollywood",
                songs: [
                    { title: "Tum Hi Ho", src: "https://archive.org/download/testmp3_202108/testmp3.mp3" },
                    { title: "Kal Ho Naa Ho", src: "https://archive.org/download/sample_mp3_64kb/sample_mp3_64kb.mp3" }
                ]
            },
            {
                album: "Chill Vibes",
                art: "https://via.placeholder.com/120?text=Chill",
                songs: [
                    { title: "Soft Breeze", src: "https://archive.org/download/testmp3_202108/testmp3.mp3" },
                    { title: "Night Calm", src: "https://archive.org/download/sample_mp3_64kb/sample_mp3_64kb.mp3" }
                ]
            }
        ];

        let currentPlaylist = playlists[0];
        let currentSongIndex = 0;

        function loadSong(playlist, index) {
            const song = playlist.songs[index];
            audioPlayer.src = song.src;
            songTitle.textContent = song.title;
            albumArt.src = playlist.art;
            widget.classList.add('playing');
            currentPlaylist = playlist;
            currentSongIndex = index;
        }

        function togglePlayPause() {
            if (isPlaying) {
                audioPlayer.pause();
                document.querySelector('#controls button:nth-child(2)').textContent = '▶️';
            } else {
                audioPlayer.play();
                document.querySelector('#controls button:nth-child(2)').textContent = '⏸';
            }
            isPlaying = !isPlaying;
        }

        function nextSong() {
            currentSongIndex = (currentSongIndex + 1) % currentPlaylist.songs.length;
            loadSong(currentPlaylist, currentSongIndex);
            if (isPlaying) audioPlayer.play();
        }

        function previousSong() {
            currentSongIndex = (currentSongIndex - 1 + currentPlaylist.songs.length) % currentPlaylist.songs.length;
            loadSong(currentPlaylist, currentSongIndex);
            if (isPlaying) audioPlayer.play();
        }

        albumArt.addEventListener('click', () => {
            albumName.textContent = currentPlaylist.album;
            albumSongs.innerHTML = '';
            currentPlaylist.songs.forEach((song, index) => {
                const li = document.createElement('li');
                li.textContent = song.title;
                li.addEventListener('click', () => {
                    loadSong(currentPlaylist, index);
                    albumList.style.display = 'none';
                    audioPlayer.play();
                    isPlaying = true;
                    document.querySelector('#controls button:nth-child(2)').textContent = '⏸';
                });
                albumSongs.appendChild(li);
            });
            albumList.style.display = 'block';
        });

        // Close album list if clicked outside
        widget.addEventListener('click', (e) => {
            if (e.target !== albumArt && !albumList.contains(e.target)) {
                albumList.style.display = 'none';
            }
        });

        // Load first song by default (not playing)
        loadSong(playlists[0], 0);
        widget.classList.remove('playing');
    </script>
</body>
</html>